apiVersion: nais.io/v1
kind: Naisjob
metadata:
  name: attest-sign-integration-test
  namespace: nais-verification
spec:
  image: {{ image }}
  port: 8080
  ttl: 5m
  replicas:
    max: 1
    min: 1
  resources:
    requests:
      cpu: 2m
      memory: 8Mi